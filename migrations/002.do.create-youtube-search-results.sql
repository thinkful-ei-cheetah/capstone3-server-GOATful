CREATE TABLE youtube_search_results (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  data TEXT,
  video_id INTEGER REFERENCES videos(id) ON DELETE CASCADE NOT NULL,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);